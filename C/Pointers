#include <stdio.h>

int main() {
    int num1, num2, sum;
    int *a, *b;
    
    a = &num1;
    b = &num2;
    
    printf("Введите два числа: ");
    scanf("%d %d", a, b);
    
    sum = *a + *b;
    printf("Сумма = %d", sum);
    
    return 0;
}
