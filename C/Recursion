#include <stdio.h>

int fact(int n) {
	if (n == 1) {
    	return 1;
    }
    return fact(n - 1) * n;
}

int main() {
    int number;
    
    printf("Введите число: ");
    scanf("%i", &number);
    printf("Факториал числа %i равен %i", number, fact(number));
    
    return 0;
}
